Placeholder. The App Store listing (description, keywords, screenshots,
privacy details) is managed in App Store Connect for now, and every lane
runs `deliver` with `skip_metadata` and `skip_screenshots`.
